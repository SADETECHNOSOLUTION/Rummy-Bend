spring:
  application:
    name: USER-INFO

  data:
    mongodb:
      uri: mongodb+srv://sreemanvk:Sree2001@cluster0.pvohmlz.mongodb.net/user_authentication?retryWrites=true&w=majority
      database: user_authentication

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_USER}
    password: ${EMAIL_PASS}

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

  redis:
    url: ${REDIS_URL}

logging:
  file:
    name: logs/user-info.log
  pattern:
    level: "%5p [${spring.application.name:},traceId=%X{traceId:-},spanId=%X{spanId:-}]"
    file: "%d{yyyy-MM-dd HH:mm:ss} %-5level [${spring.application.name:},traceId=%X{traceId:-},spanId=%X{spanId:-}] [%thread] %logger{36} - %msg%n"


server:
  port: ${PORT:10000}
  address: 0.0.0.0

wallet:
  id: ${WALLET_ID:default_wallet_id}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
#  instance:
#    # ADD THIS LINE: It forces the ID to match your public URL
#    instance-id: ${spring.application.name}:${server.port}
#    prefer-ip-address: false
##    non-secure-port: ${SERVER_PORT:10000}
##    hostname: rummy-userinfo-v1-1.onrender.com
#    status-page-url-path: /actuator/info
#    health-check-url-path: /actuator/health
    
jwt:
  secret: ${JWT_SECRET:mySuperSecretKeyForLocalDevelopment1234567890}








