spring:
  application:
    name: chatbot

  data:
    mongodb:
      uri: mongodb+srv://sreemanvk:Msg@2709@cluster0.pvohmlz.mongodb.net/?appName=Cluster0
      database: chatbot

management:
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: "http://localhost:9411/api/v2/spans"

logging:
  pattern:
    level: "%5p [${spring.application.name:},traceId=%X{traceId:-},spanId=%X{spanId:-}]"


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka
    fetch-registry: true
    register-with-eureka: true

server:
  port: 7081